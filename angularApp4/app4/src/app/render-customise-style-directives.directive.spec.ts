import { RenderCustomiseStyleDirectivesDirective } from './render-customise-style-directives.directive';

describe('RenderCustomiseStyleDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderCustomiseStyleDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
