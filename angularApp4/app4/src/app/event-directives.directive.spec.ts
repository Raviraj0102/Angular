import { EventDirectivesDirective } from './event-directives.directive';

describe('EventDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new EventDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
