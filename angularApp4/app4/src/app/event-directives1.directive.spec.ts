import { EventDirectives1Directive } from './event-directives1.directive';

describe('EventDirectives1Directive', () => {
  it('should create an instance', () => {
    const directive = new EventDirectives1Directive();
    expect(directive).toBeTruthy();
  });
});
